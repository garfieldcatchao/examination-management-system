# 摄像头预览功能说明

## 新增的预览功能

我已经为用户监控组件添加了实时摄像头预览功能，现在用户可以看到摄像头的实际画面状态。

## 功能特性

### 1. 实时视频预览
- ✅ 显示摄像头的实时画面
- ✅ 镜像显示（更符合用户习惯）
- ✅ 自适应尺寸（280×270像素容器内的80px高度预览区域）
- ✅ 视频流自动播放和同步

### 2. 人脸检测可视化
- ✅ 实时人脸检测状态指示器
- ✅ 检测到人脸时显示绿色边框
- ✅ 人脸检测框动画效果
- ✅ "人脸已识别"标签显示

### 3. 状态覆盖层
- ✅ 渐变背景覆盖层，不影响视频观看
- ✅ 右上角状态指示器（绿色/红色）
- ✅ 底部状态文字（已检测/未检测，在线/离线）
- ✅ 脉冲动画效果

### 4. 无预览状态
- ✅ 摄像头未开启时显示占位符
- ✅ 引导用户开启监控的提示
- ✅ 统一的视觉设计

## 核心组件

### CameraPreview 组件

```tsx
const CameraPreview: React.FC<{
  videoElement: HTMLVideoElement | null;
  faceDetected: boolean;
  userPresent: boolean;
}> = ({ videoElement, faceDetected, userPresent }) => {
  // 实现视频流同步和状态显示
}
```

**主要功能：**
- 接收video元素并同步播放
- 显示人脸检测状态
- 提供视觉反馈和状态指示

## 视觉效果

### 1. 视频显示
```css
video {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transform: scaleX(-1); /* 镜像显示 */
}
```

### 2. 人脸检测框
- 检测到人脸时在中央显示绿色边框
- 边框带有脉冲动画效果
- 顶部显示"人脸已识别"标签

### 3. 状态指示器
- 右上角8px圆形指示器
- 绿色：检测到人脸
- 红色：未检测到人脸
- 带有光晕效果

### 4. 底部状态栏
- 左侧：人脸检测状态（👤 已检测 / ❓ 未检测）
- 右侧：用户状态（在线/离线，颜色编码）

## 技术实现

### 1. 视频流同步
```tsx
React.useEffect(() => {
  if (videoElement && videoRef) {
    // 同步video元素的流到预览组件
    videoRef.srcObject = videoElement.srcObject;
    videoRef.play().catch(console.error);
  }
}, [videoElement, videoRef]);
```

### 2. 状态响应
- 实时响应`faceDetected`状态变化
- 动态更新视觉指示器
- 平滑的状态转换动画

### 3. 性能优化
- 复用现有的video元素流
- 避免重复的媒体设备访问
- 优化CSS动画性能

## 用户体验

### 1. 视觉反馈
- 清晰的状态指示
- 平滑的动画过渡
- 直观的颜色编码

### 2. 隐私保护
- 预览仅显示给用户本人
- 不影响监控数据传输
- 状态信息透明展示

### 3. 交互友好
- 自动播放无需用户操作
- 错误状态明确提示
- 一致的设计语言

## 使用方法

### 1. 开启监控
1. 点击"📹 开始监控"按钮
2. 授权摄像头权限
3. 预览区域自动显示摄像头画面

### 2. 状态监控
1. 观察右上角状态指示器
2. 查看底部状态文字
3. 注意人脸检测框的出现

### 3. 行为反馈
- 人脸检测成功：绿色指示器 + 检测框
- 用户离开：红色指示器 + 离线状态
- 异常情况：相应的警报提示

## 故障排除

### 1. 预览不显示
- 检查摄像头权限是否授权
- 确认摄像头设备正常工作
- 查看浏览器控制台错误信息

### 2. 人脸检测不准确
- 调整光线环境
- 确保正对摄像头
- 检查摄像头分辨率设置

### 3. 画面延迟或卡顿
- 检查网络连接
- 关闭其他占用摄像头的应用
- 降低拍照频率设置

## 配置选项

可以通过修改配置来调整预览效果：

```tsx
const userMonitoring = useUserMonitoring(examId, userId, {
  maxWidth: 640,        // 摄像头分辨率
  maxHeight: 480,
  captureInterval: 2000, // 检测频率
  enableFaceDetection: true,
  enableBehaviorAnalysis: true,
});
```

## 浏览器兼容性

- ✅ Chrome 53+
- ✅ Firefox 36+  
- ✅ Safari 11+
- ✅ Edge 79+

**注意：** 需要HTTPS环境或localhost才能访问摄像头。

## 更新内容

1. **新增 CameraPreview 组件**：负责视频预览和状态显示
2. **优化 CSS 动画**：添加人脸检测和状态指示动画
3. **改进用户体验**：实时预览让用户更好地了解监控状态
4. **增强视觉反馈**：丰富的状态指示和动画效果

现在用户可以实时查看摄像头画面，清楚地了解监控系统的工作状态，包括人脸检测结果和用户在线状态。
