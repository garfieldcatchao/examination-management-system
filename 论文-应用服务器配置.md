# 应用服务器配置与运行环境

## 2.X 系统架构与服务器配置

### 2.X.1 系统架构设计

本考试管理系统采用前后端分离的分布式架构，系统由前端应用服务器、后端API服务器、WebSocket实时通信服务器和数据库服务器四个核心组件构成。各组件之间通过HTTP协议和WebSocket协议进行通信，实现了业务逻辑与数据展示的分离，提高了系统的可扩展性和可维护性。

系统架构如图2.X所示，前端React应用运行在Webpack开发服务器上，通过HTTP请求与后端API服务器交互，通过WebSocket协议与实时监控服务器建立长连接。后端API服务器基于Spring Boot框架构建，提供RESTful风格的API接口。WebSocket服务器使用Node.js实现，负责处理考试监控数据的实时传输。MySQL数据库作为数据持久化层，存储系统的所有业务数据。

```
┌─────────────────────────────────────────────────────────┐
│                     客户端浏览器                          │
│              (React + TypeScript 应用)                    │
└──────────────┬──────────────────────────┬─────────────────┘
               │                          │
               │ HTTP/HTTPS               │ WebSocket
               │                          │
    ┌──────────▼──────────┐    ┌─────────▼──────────┐
    │   前端开发服务器      │    │  WebSocket服务器     │
    │   (端口: 3000)       │    │   (端口: 8080)       │
    │  Webpack Dev Server  │    │  Node.js + ws       │
    └──────────┬──────────┘    └─────────────────────┘
               │
               │ HTTP Proxy
               │
    ┌──────────▼──────────┐
    │   后端API服务器      │
    │   (端口: 8083)       │
    │  Spring Boot        │
    └──────────┬──────────┘
               │
               │ JDBC
               │
    ┌──────────▼──────────┐
    │   MySQL数据库服务器   │
    │   (端口: 3306)       │
    └─────────────────────┘
```

**图2.X 系统架构图**

### 2.X.2 前端应用服务器配置

前端应用服务器采用Webpack Dev Server作为开发环境服务器，负责提供React应用的运行环境和开发工具支持。

#### 2.X.2.1 服务器配置参数

前端开发服务器的主要配置参数如下：

- **监听端口**: 3000（默认端口，可通过环境变量PORT自定义）
- **监听地址**: 0.0.0.0（允许外部访问）
- **热模块替换**: 启用（HMR，Hot Module Replacement）
- **源代码映射**: 启用（Source Maps，便于调试）
- **自动打开浏览器**: 启用（开发环境）

#### 2.X.2.2 代理配置

前端服务器通过HTTP代理将API请求转发到后端服务器，解决了开发环境下的跨域问题。代理配置通过`src/setupProxy.js`文件实现，所有以`/api`开头的请求都会被代理到`http://localhost:8083`，并自动移除`/api`前缀。

代理配置的核心代码如下：

```javascript
app.use(
  "/api",
  createProxyMiddleware({
    target: "http://localhost:8083",
    changeOrigin: true,
    pathRewrite: {
      "^/api": "",
    }
  })
);
```

#### 2.X.2.3 构建配置

生产环境的前端应用通过Webpack进行打包构建，生成静态文件部署到Web服务器。构建配置支持代码分割、资源压缩、Tree Shaking等优化策略，确保生产环境的性能。

### 2.X.3 后端API服务器配置

后端API服务器基于Spring Boot框架构建，提供RESTful风格的API接口服务。

#### 2.X.3.1 服务器配置

- **监听端口**: 8083（可通过application.properties配置）
- **应用上下文路径**: `/`（根路径）
- **字符编码**: UTF-8
- **JSON序列化**: Jackson（默认）

#### 2.X.3.2 数据库连接配置

后端服务器通过JPA/Hibernate与MySQL数据库建立连接，连接配置如下：

```properties
spring.datasource.url=jdbc:mysql://localhost:3306/exam_management?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
```

#### 2.X.3.3 跨域配置

为支持前后端分离架构，后端服务器配置了CORS（Cross-Origin Resource Sharing）跨域资源共享策略，允许前端应用访问API接口。

#### 2.X.3.4 文件上传配置

系统支持题目和试卷的批量导入功能，后端服务器配置了文件上传功能，主要参数如下：

- **最大文件大小**: 10MB
- **存储路径**: `uploads/`目录
- **支持格式**: Excel (.xlsx)、Word (.docx)、图片等

### 2.X.4 WebSocket实时通信服务器配置

WebSocket服务器使用Node.js和ws库实现，负责处理考试监控数据的实时传输。

#### 2.X.4.1 服务器配置

- **监听端口**: 8080
- **WebSocket路径**: `/user-monitoring`
- **协议**: WebSocket (ws://) 或 WebSocket Secure (wss://)
- **连接管理**: 支持多客户端并发连接

#### 2.X.4.2 消息协议设计

WebSocket服务器定义了统一的消息格式，所有消息采用JSON格式传输，包含以下字段：

- `type`: 消息类型（auth、user_photo、user_activity等）
- `data`: 消息数据（根据类型不同而不同）
- `timestamp`: 时间戳

#### 2.X.4.3 连接管理机制

服务器维护一个客户端连接映射表，记录每个连接的客户端ID、用户ID、考试ID等信息。当客户端断开连接时，自动清理相关资源。服务器还实现了心跳检测机制，定期检查连接状态，及时清理无效连接。

### 2.X.5 数据库服务器配置

MySQL数据库作为系统的数据持久化层，存储所有业务数据。

#### 2.X.5.1 数据库配置

- **数据库名称**: exam_management
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_0900_ai_ci
- **存储引擎**: InnoDB（支持事务和外键约束）
- **默认端口**: 3306

#### 2.X.5.2 数据库表结构

系统设计了完整的数据表结构，主要包括：

1. **用户相关表**: users（用户表）、user_classes（用户班级关联表）、user_settings（用户设置表）
2. **考试相关表**: examinations（考试表）、exam_participants（考试参与表）、exam_records（考试记录表）、exam_answers（考试答案表）
3. **题库相关表**: questions（题目表）、question_options（选项表）、question_tags（题目标签表）
4. **试卷相关表**: exam_paper（试卷表）、paper_questions（试卷题目关联表）
5. **监控相关表**: exam_monitoring_records（监控记录表）、exam_monitoring_snapshots（监控快照表）、exam_behavior_logs（行为日志表）

#### 2.X.5.3 索引优化

为提高查询性能，系统在关键字段上建立了索引：

- 主键索引：所有表的主键字段
- 外键索引：关联字段建立索引
- 复合索引：如`(user_id, exam_id)`、`(exam_id, user_id)`等常用查询组合

### 2.X.6 环境变量配置

系统使用环境变量管理不同环境下的配置，通过`.env`文件定义环境变量。

#### 2.X.6.1 环境变量定义

主要环境变量包括：

- `BACKEND_API_URL`: 后端API基础地址（默认：http://localhost:8080/api）
- `NODE_ENV`: 运行环境（development/production）
- `DB_HOST`: 数据库主机地址
- `DB_PORT`: 数据库端口
- `DB_NAME`: 数据库名称
- `DB_USER`: 数据库用户名
- `DB_PASSWORD`: 数据库密码

#### 2.X.6.2 环境变量加载

前端应用通过`config/env.js`加载环境变量，Webpack在构建时将环境变量注入到应用代码中。后端应用通过Spring Boot的`application.properties`或`application.yml`文件加载配置，支持多环境配置（dev、test、prod）。

### 2.X.7 开发环境配置

开发环境配置旨在提供便捷的开发体验和高效的调试支持。

#### 2.X.7.1 开发工具链

- **前端开发**: Cursor编辑器 + React DevTools
- **后端开发**: IntelliJ IDEA + Spring Boot DevTools
- **数据库管理**: MySQL Workbench
- **API测试**: Postman

#### 2.X.7.2 热更新机制

前端开发服务器支持热模块替换（HMR），当代码文件修改时，浏览器自动更新修改的部分，无需刷新整个页面，大大提高了开发效率。

#### 2.X.7.3 Mock数据服务

开发环境使用Mock服务器模拟后端API，使前端开发不依赖后端服务，提高了开发并行度和效率。

### 2.X.8 生产环境部署配置

生产环境配置注重性能、安全性和稳定性。

#### 2.X.8.1 前端部署

前端应用通过`npm run build`命令构建生产版本，生成优化后的静态文件。部署方案包括：

1. **Nginx部署**: 将构建产物部署到Nginx服务器，配置反向代理和静态资源缓存
2. **CDN加速**: 将静态资源部署到CDN，提高访问速度
3. **HTTPS配置**: 使用SSL证书启用HTTPS，保证数据传输安全

#### 2.X.8.2 后端部署

后端应用通过Maven打包成JAR文件，部署方案包括：

1. **独立运行**: 使用`java -jar`命令直接运行JAR文件
2. **进程管理**: 使用PM2或systemd管理应用进程，支持自动重启和日志管理
3. **容器化部署**: 使用Docker容器化部署，便于扩展和管理

#### 2.X.8.3 WebSocket服务器部署

WebSocket服务器使用PM2进行进程管理，支持集群模式提高并发处理能力。配置了自动重启、日志管理和监控功能。

#### 2.X.8.4 数据库优化

生产环境数据库配置优化包括：

- **连接池配置**: 设置合适的连接池大小，避免连接数过多或过少
- **查询优化**: 建立合适的索引，优化慢查询
- **备份策略**: 定期备份数据库，保证数据安全
- **读写分离**: 如需要，可配置主从复制实现读写分离

### 2.X.9 安全配置

系统在多个层面配置了安全措施，保证系统安全运行。

#### 2.X.9.1 网络安全

- **HTTPS**: 生产环境启用HTTPS，加密数据传输
- **CORS策略**: 限制允许的域名，防止跨站请求攻击
- **防火墙**: 配置防火墙规则，只开放必要端口

#### 2.X.9.2 应用安全

- **SQL注入防护**: 使用参数化查询，防止SQL注入攻击
- **XSS防护**: 对用户输入进行转义和验证
- **密码加密**: 使用BCrypt算法加密用户密码
- **Token认证**: 使用JWT Token进行身份认证，设置合理的过期时间

#### 2.X.9.3 数据安全

- **敏感数据加密**: 对敏感数据进行加密存储
- **访问控制**: 实现基于角色的访问控制（RBAC）
- **审计日志**: 记录关键操作日志，便于审计和追溯

### 2.X.10 监控与日志

系统配置了完善的监控和日志机制，便于问题排查和性能优化。

#### 2.X.10.1 日志配置

- **前端日志**: 使用浏览器控制台和错误监控服务记录前端错误
- **后端日志**: 使用Logback记录应用日志，支持日志级别和文件滚动
- **WebSocket日志**: 记录连接和消息日志，便于排查问题

#### 2.X.10.2 监控指标

系统监控的关键指标包括：

- **服务器资源**: CPU使用率、内存使用率、磁盘空间
- **应用性能**: API响应时间、错误率、并发连接数
- **数据库性能**: 连接数、查询性能、慢查询统计

### 2.X.11 本章小结

本章详细介绍了考试管理系统的服务器配置和运行环境。系统采用前后端分离架构，包含前端应用服务器、后端API服务器、WebSocket实时通信服务器和MySQL数据库服务器四个核心组件。各服务器通过合理的配置实现了高效、安全、稳定的运行。开发环境配置注重开发效率和调试便利性，生产环境配置注重性能、安全性和稳定性。通过完善的监控和日志机制，系统能够及时发现和解决问题，保证系统的可靠运行。

