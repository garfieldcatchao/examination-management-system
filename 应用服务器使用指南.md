# 应用服务器使用指南

## 目录
1. [快速开始](#快速开始)
2. [服务器组件说明](#服务器组件说明)
3. [开发环境使用](#开发环境使用)
4. [生产环境使用](#生产环境使用)
5. [服务器管理](#服务器管理)
6. [常见问题](#常见问题)

---

## 快速开始

### 前置准备

1. **安装 Node.js** (>= 14.0.0)
```bash
# 检查Node.js版本
node -v

# 检查npm版本
npm -v
```

2. **安装项目依赖**
```bash
cd /Users/apple/Documents/examination-management-system
npm install
```

3. **配置环境变量**（可选）
```bash
# 复制环境变量示例文件
cp env.example .env

# 编辑.env文件，根据实际情况修改配置
```

### 一键启动（推荐）

```bash
# 同时启动所有开发服务器（前端、WebSocket、Mock API）
npm run dev
```

这将启动：
- ✅ 前端开发服务器（端口 3000）
- ✅ WebSocket 服务器（端口 8080）
- ✅ Mock API 服务器（端口 8083）

---

## 服务器组件说明

### 1. 前端开发服务器

**作用**: 提供 React 应用的开发环境，支持热更新

**端口**: 3000

**启动命令**:
```bash
npm start
```

**访问地址**: http://localhost:3000

**功能**:
- 自动编译 TypeScript/JavaScript
- 热模块替换（HMR）
- 自动代理 API 请求到后端
- 源代码映射（Source Maps）

**停止**: 在终端按 `Ctrl + C`

---

### 2. WebSocket 服务器

**作用**: 提供实时监控数据传输服务

**端口**: 8080

**WebSocket 路径**: `/user-monitoring`

**启动命令**:
```bash
npm run ws-server
```

**访问地址**: `ws://localhost:8080/user-monitoring`

**功能**:
- 实时监控数据传输
- 考试监控快照上传
- 行为日志传输
- 多客户端连接管理

**停止**: 在终端按 `Ctrl + C`

**验证是否运行**:
```bash
# 检查端口是否被占用
lsof -i :8080

# 或使用curl测试（需要WebSocket客户端工具）
```

---

### 3. Mock API 服务器（开发环境）

**作用**: 模拟后端 API，用于前端开发测试

**端口**: 8083

**启动命令**:
```bash
npm run mock-api
```

**访问地址**: http://localhost:8083

**功能**:
- 提供模拟的 RESTful API
- 文件上传接口
- 登录认证接口
- 数据查询接口

**停止**: 在终端按 `Ctrl + C`

**验证是否运行**:
```bash
# 测试API是否可用
curl http://localhost:8083/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"studentId":"test","password":"123"}'
```

---

### 4. Spring Boot 后端服务器（生产环境）

**作用**: 提供真实的后端 API 服务

**端口**: 8083（或配置的端口）

**启动方式**:

#### 方式一：使用 Maven
```bash
# 在项目根目录（如果有pom.xml）
mvn spring-boot:run
```

#### 方式二：使用 IDEA
1. 打开 IDEA
2. 找到主启动类（通常有 `@SpringBootApplication` 注解）
3. 右键 → Run

#### 方式三：运行 JAR 包
```bash
# 先打包
mvn clean package

# 运行JAR
java -jar target/exam-management-system-1.0.0.jar
```

**访问地址**: http://localhost:8083

---

## 开发环境使用

### 场景一：完整开发环境（推荐）

**适用**: 需要同时开发前端和后端功能

**启动步骤**:

1. **启动数据库**（如果使用本地 MySQL）
```bash
# Mac (使用Homebrew)
brew services start mysql

# Linux
sudo systemctl start mysql

# Windows
# 在服务管理器中启动MySQL服务
```

2. **启动所有开发服务器**
```bash
npm run dev
```

3. **访问应用**
- 前端: http://localhost:3000
- API文档: http://localhost:8083

### 场景二：仅前端开发

**适用**: 只开发前端功能，使用 Mock API

**启动步骤**:

```bash
# 终端1: 启动前端
npm start

# 终端2: 启动Mock API
npm run mock-api
```

### 场景三：仅测试 WebSocket 功能

**启动步骤**:

```bash
# 启动WebSocket服务器
npm run ws-server

# 在浏览器控制台测试连接
const ws = new WebSocket('ws://localhost:8080/user-monitoring');
ws.onopen = () => console.log('连接成功');
ws.onmessage = (e) => console.log('收到消息:', e.data);
```

---

## 生产环境使用

### 1. 前端部署

#### 步骤一：构建生产版本
```bash
npm run build
```

构建产物在 `build/` 目录

#### 步骤二：部署到服务器

**使用 Nginx**:
```bash
# 复制构建文件到Nginx目录
cp -r build/* /usr/share/nginx/html/exam-system/

# 配置Nginx（见服务器配置文档）
```

**使用 Node.js 服务器**:
```bash
# 安装serve
npm install -g serve

# 启动静态文件服务器
serve -s build -l 3000
```

### 2. 后端部署

#### 使用 JAR 包运行
```bash
# 打包
mvn clean package

# 运行（后台运行）
nohup java -jar target/exam-management-system-1.0.0.jar > app.log 2>&1 &

# 查看日志
tail -f app.log
```

#### 使用 systemd 管理（Linux）
```bash
# 创建服务文件
sudo nano /etc/systemd/system/exam-system.service

# 内容：
[Unit]
Description=Exam Management System
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/exam-system
ExecStart=/usr/bin/java -jar /opt/exam-system/app.jar
Restart=always

[Install]
WantedBy=multi-user.target

# 启动服务
sudo systemctl start exam-system
sudo systemctl enable exam-system
```

### 3. WebSocket 服务器部署

#### 使用 PM2 管理（推荐）
```bash
# 安装PM2
npm install -g pm2

# 启动服务
pm2 start websocket-server.js --name ws-server

# 查看状态
pm2 status

# 查看日志
pm2 logs ws-server

# 设置开机自启
pm2 startup
pm2 save

# 停止服务
pm2 stop ws-server

# 重启服务
pm2 restart ws-server
```

#### 使用 systemd 管理
```bash
# 创建服务文件
sudo nano /etc/systemd/system/ws-server.service

# 内容：
[Unit]
Description=WebSocket Server
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/exam-system
ExecStart=/usr/bin/node /opt/exam-system/websocket-server.js
Restart=always

[Install]
WantedBy=multi-user.target

# 启动服务
sudo systemctl start ws-server
sudo systemctl enable ws-server
```

---

## 服务器管理

### 检查服务器状态

#### 检查端口占用
```bash
# Mac/Linux
lsof -i :3000  # 前端
lsof -i :8080  # WebSocket
lsof -i :8083  # 后端API

# Windows
netstat -ano | findstr :3000
```

#### 检查进程
```bash
# Mac/Linux
ps aux | grep node
ps aux | grep java

# Windows
tasklist | findstr node
tasklist | findstr java
```

### 停止服务器

#### 开发环境
- 在运行服务器的终端按 `Ctrl + C`

#### 生产环境
```bash
# 使用PM2
pm2 stop all
pm2 delete all

# 使用systemd
sudo systemctl stop exam-system
sudo systemctl stop ws-server

# 杀死进程（不推荐）
kill -9 <PID>
```

### 查看日志

#### 开发环境
- 日志直接输出到终端

#### 生产环境
```bash
# PM2日志
pm2 logs

# systemd日志
sudo journalctl -u exam-system -f
sudo journalctl -u ws-server -f

# 应用日志文件
tail -f app.log
```

---

## 常见问题

### 问题1: 端口被占用

**错误信息**: `Error: listen EADDRINUSE: address already in use :::3000`

**解决方法**:
```bash
# 1. 查找占用端口的进程
lsof -i :3000

# 2. 杀死进程
kill -9 <PID>

# 3. 或修改端口
PORT=3001 npm start
```

### 问题2: 依赖安装失败

**解决方法**:
```bash
# 清除缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题3: WebSocket 连接失败

**检查清单**:
1. WebSocket 服务器是否启动
2. 端口 8080 是否被占用
3. 防火墙是否允许连接
4. 浏览器控制台是否有错误

**测试连接**:
```javascript
// 在浏览器控制台运行
const ws = new WebSocket('ws://localhost:8080/user-monitoring');
ws.onopen = () => console.log('✅ 连接成功');
ws.onerror = (e) => console.error('❌ 连接失败', e);
ws.onclose = () => console.log('连接关闭');
```

### 问题4: API 请求失败

**检查清单**:
1. 后端服务是否启动
2. 代理配置是否正确（`src/setupProxy.js`）
3. CORS 配置是否正确
4. 网络请求是否被拦截

**测试 API**:
```bash
# 测试登录接口
curl -X POST http://localhost:8083/auth/login \
  -H "Content-Type: application/json" \
  -d '{"studentId":"test","password":"123"}'
```

### 问题5: 数据库连接失败

**检查清单**:
1. MySQL 服务是否启动
2. 数据库是否已创建
3. 用户名密码是否正确
4. 连接字符串是否正确

**测试连接**:
```bash
# 测试MySQL连接
mysql -u root -p -h localhost exam_management
```

### 问题6: 文件上传失败

**检查清单**:
1. `uploads/` 目录是否存在且有写权限
2. 文件大小是否超过限制（默认 10MB）
3. 文件类型是否允许

**解决方法**:
```bash
# 创建上传目录
mkdir -p uploads
chmod 755 uploads
```

---

## 性能优化建议

### 开发环境

1. **使用并发启动**: `npm run dev` 同时启动多个服务
2. **启用缓存**: Webpack 缓存可以加速编译
3. **减少日志输出**: 生产环境关闭详细日志

### 生产环境

1. **使用 PM2 集群模式**:
```bash
pm2 start websocket-server.js -i 4  # 启动4个实例
```

2. **启用 Gzip 压缩**: 在 Nginx 中配置
3. **使用 CDN**: 静态资源使用 CDN 加速
4. **数据库连接池**: 配置合适的连接池大小

---

## 监控和维护

### 监控指标

1. **服务器资源**
   - CPU 使用率
   - 内存使用率
   - 磁盘空间

2. **应用指标**
   - 请求响应时间
   - 错误率
   - 并发连接数

3. **数据库指标**
   - 连接数
   - 查询性能
   - 慢查询日志

### 定期维护

1. **日志清理**: 定期清理旧日志文件
2. **数据库备份**: 定期备份数据库
3. **依赖更新**: 定期更新依赖包（注意兼容性）
4. **安全检查**: 定期检查安全漏洞

---

## 快速参考命令

### 开发环境

```bash
# 启动所有服务
npm run dev

# 仅启动前端
npm start

# 仅启动WebSocket
npm run ws-server

# 仅启动Mock API
npm run mock-api
```

### 生产环境

```bash
# 构建前端
npm run build

# 启动后端（JAR）
java -jar app.jar

# 使用PM2管理
pm2 start websocket-server.js --name ws-server
pm2 start app.js --name api-server

# 查看状态
pm2 status
pm2 logs
```

### 检查命令

```bash
# 检查端口
lsof -i :3000
lsof -i :8080
lsof -i :8083

# 检查进程
ps aux | grep node
ps aux | grep java

# 测试API
curl http://localhost:8083/auth/login -X POST -H "Content-Type: application/json" -d '{"studentId":"test","password":"123"}'
```

---

## 总结

1. **开发环境**: 使用 `npm run dev` 一键启动所有服务
2. **生产环境**: 分别部署前端、后端和 WebSocket 服务器
3. **管理工具**: 使用 PM2 或 systemd 管理 Node.js 服务
4. **监控**: 定期检查服务器状态和日志
5. **维护**: 定期备份和更新

如有问题，请参考"服务器与运行环境配置.md"文档或查看项目 README。

