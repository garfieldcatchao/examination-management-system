# 服务器与运行环境配置

## 1. 系统架构概述

本考试管理系统采用前后端分离的架构，包含以下服务组件：

```
┌─────────────────┐
│   前端应用       │  React + TypeScript
│   (端口: 3000)   │
└────────┬─────────┘
         │ HTTP/WebSocket
         │
┌────────┴─────────────────────────────────────┐
│                                               │
│  ┌──────────────┐  ┌──────────────┐         │
│  │ 后端API服务   │  │ WebSocket服务 │         │
│  │ (端口: 8083)  │  │ (端口: 8080)  │         │
│  └──────┬───────┘  └──────┬───────┘         │
│         │                 │                  │
│         └────────┬────────┘                  │
│                  │                            │
│         ┌────────┴────────┐                  │
│         │   MySQL数据库    │                  │
│         │   (端口: 3306)   │                  │
│         └─────────────────┘                  │
└───────────────────────────────────────────────┘
```

## 2. 服务器端口配置

### 2.1 前端开发服务器

- **端口**: 3000（默认）
- **配置文件**: `config/webpackDevServer.config.js`
- **启动命令**: `npm start`
- **访问地址**: `http://localhost:3000`

**配置说明**:
- 使用 Webpack Dev Server 提供开发环境服务
- 支持热模块替换（HMR）
- 自动代理后端 API 请求到 `http://localhost:8083`
- 支持 HTTPS（可选，通过 `getHttpsConfig.js` 配置）

### 2.2 后端 API 服务器

- **端口**: 8083
- **配置文件**: `mock-server.js`（开发环境）或 Spring Boot 配置（生产环境）
- **启动方式**: 
  - 开发环境: `npm run mock-api`
  - 生产环境: Spring Boot 应用
- **访问地址**: `http://localhost:8083`

**配置说明**:
- 开发环境使用 Express.js 模拟后端服务
- 生产环境使用 Spring Boot 提供 RESTful API
- 支持 CORS 跨域请求
- 文件上传限制: 10MB

### 2.3 WebSocket 服务器

- **端口**: 8080
- **配置文件**: `websocket-server.js`
- **启动命令**: `npm run ws-server`
- **WebSocket 路径**: `/user-monitoring`
- **访问地址**: `ws://localhost:8080/user-monitoring`

**配置说明**:
- 用于实时监控数据传输
- 支持考试监控快照上传
- 支持多客户端连接管理
- 自动重连机制

### 2.4 数据库服务器

- **数据库类型**: MySQL
- **默认端口**: 3306
- **字符集**: utf8mb4
- **排序规则**: utf8mb4_0900_ai_ci
- **存储引擎**: InnoDB

## 3. 环境变量配置

### 3.1 环境变量文件

项目使用 `.env` 文件管理环境变量，参考 `env.example` 文件：

```bash
# 后端API地址配置
BACKEND_API_URL=http://localhost:8080/api

# OSS配置（可选，如果使用OSS存储图片）
# OSS_REGION=oss-cn-hangzhou
# OSS_ACCESS_KEY_ID=your_access_key_id
# OSS_ACCESS_KEY_SECRET=your_access_key_secret
# OSS_BUCKET=exam-system-snapshots
```

### 3.2 创建环境变量文件

```bash
# 复制示例文件
cp env.example .env

# 编辑环境变量
# 根据实际环境修改配置
```

### 3.3 环境变量说明

| 变量名 | 说明 | 默认值 | 必需 |
|--------|------|--------|------|
| `BACKEND_API_URL` | 后端API基础地址 | `http://localhost:8080/api` | 是 |
| `NODE_ENV` | 运行环境 | `development` | 是 |
| `REACT_APP_*` | React应用环境变量 | - | 否 |
| `OSS_REGION` | OSS区域（如使用） | - | 否 |
| `OSS_ACCESS_KEY_ID` | OSS访问密钥ID | - | 否 |
| `OSS_ACCESS_KEY_SECRET` | OSS访问密钥 | - | 否 |
| `OSS_BUCKET` | OSS存储桶名称 | - | 否 |

## 4. 数据库配置

### 4.1 数据库连接配置

**Spring Boot 配置** (生产环境):
```properties
# application.properties
spring.datasource.url=jdbc:mysql://localhost:3306/exam_management?useUnicode=true&characterEncoding=utf8mb4&useSSL=false&serverTimezone=Asia/Shanghai
spring.datasource.username=root
spring.datasource.password=your_password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
```

### 4.2 数据库初始化

1. **创建数据库**:
```sql
CREATE DATABASE exam_management 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_0900_ai_ci;
```

2. **执行DDL脚本**:
```bash
# 执行SQL脚本创建表结构
mysql -u root -p exam_management < entry/sqlDDL/index.sql
```

3. **初始化数据**（可选）:
```bash
mysql -u root -p exam_management < entry/sqlDDL/insert_data.sql
```

### 4.3 数据库表结构

主要数据表包括：
- `users` - 用户表
- `examinations` - 考试表
- `questions` - 题目表
- `exam_paper` - 试卷表
- `exam_participants` - 考试参与表
- `exam_answers` - 考试答案表
- `exam_monitoring_records` - 监控记录表
- `exam_monitoring_snapshots` - 监控快照表
- `exam_behavior_logs` - 行为日志表

详细表结构见 `entry/sqlDDL/index.sql`

## 5. 开发环境配置

### 5.1 系统要求

- **Node.js**: >= 14.0.0
- **npm**: >= 6.0.0 或 **yarn**: >= 1.22.0
- **Java**: >= 8 (后端开发)
- **MySQL**: >= 5.7 或 >= 8.0
- **操作系统**: Windows / macOS / Linux

### 5.2 安装依赖

```bash
# 安装前端依赖
npm install
# 或
yarn install
```

### 5.3 启动开发环境

#### 方式一：分别启动各个服务

```bash
# 终端1: 启动前端开发服务器
npm start

# 终端2: 启动WebSocket服务器
npm run ws-server

# 终端3: 启动Mock API服务器（开发环境）
npm run mock-api

# 终端4: 启动Spring Boot后端（生产环境）
# 在IDEA中运行或使用Maven命令
mvn spring-boot:run
```

#### 方式二：使用并发启动（推荐）

```bash
# 同时启动WebSocket、Mock API和前端服务
npm run dev
```

### 5.4 开发环境访问地址

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8083
- **WebSocket**: ws://localhost:8080/user-monitoring

### 5.5 代理配置

前端开发服务器通过 `src/setupProxy.js` 配置代理：

```javascript
// API请求代理到后端服务器
app.use(
  "/api",
  createProxyMiddleware({
    target: "http://localhost:8083",
    changeOrigin: true,
    pathRewrite: {
      "^/api": "", // 移除 /api 前缀
    }
  })
);
```

## 6. 生产环境配置

### 6.1 前端构建

```bash
# 构建生产版本
npm run build

# 构建产物在 build/ 目录
```

### 6.2 前端部署

#### 方式一：使用 Nginx

```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    root /path/to/build;
    index index.html;
    
    # 前端路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API代理
    location /api {
        proxy_pass http://localhost:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    
    # WebSocket代理
    location /user-monitoring {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

#### 方式二：使用 Node.js 服务器

可以使用 Express 或 Koa 等框架部署前端静态文件。

### 6.3 后端部署

#### Spring Boot 应用部署

1. **打包应用**:
```bash
mvn clean package
```

2. **运行JAR包**:
```bash
java -jar target/exam-management-system-1.0.0.jar
```

3. **使用 systemd 管理服务** (Linux):
```ini
[Unit]
Description=Exam Management System
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/exam-system
ExecStart=/usr/bin/java -jar /opt/exam-system/app.jar
Restart=always

[Install]
WantedBy=multi-user.target
```

### 6.4 WebSocket 服务器部署

生产环境建议使用 PM2 管理 WebSocket 服务器：

```bash
# 安装PM2
npm install -g pm2

# 启动WebSocket服务
pm2 start websocket-server.js --name ws-server

# 设置开机自启
pm2 startup
pm2 save
```

### 6.5 数据库优化配置

生产环境 MySQL 配置建议：

```ini
[mysqld]
# 字符集
character-set-server=utf8mb4
collation-server=utf8mb4_0900_ai_ci

# 连接数
max_connections=500
max_connect_errors=1000

# 缓冲池大小（根据服务器内存调整）
innodb_buffer_pool_size=2G

# 日志
slow_query_log=1
slow_query_log_file=/var/log/mysql/slow.log
long_query_time=2
```

## 7. 目录结构说明

```
examination-management-system/
├── build/                  # 生产构建输出目录
├── config/                 # Webpack配置文件
│   ├── webpack.config.js
│   ├── webpackDevServer.config.js
│   └── env.js
├── entry/                  # 后端实体类（Java）
│   └── sqlDDL/            # 数据库DDL脚本
├── public/                 # 静态资源目录
├── src/                    # 前端源代码
│   ├── actions/           # Redux actions
│   ├── components/       # React组件
│   ├── server/            # API请求封装
│   └── setupProxy.js      # 开发代理配置
├── uploads/                # 文件上传目录
├── user-monitoring-data/  # 监控数据存储目录
├── websocket-server.js     # WebSocket服务器
├── mock-server.js          # Mock API服务器
├── .env                    # 环境变量文件（需创建）
├── env.example             # 环境变量示例
└── package.json            # 项目依赖配置
```

## 8. 常见问题排查

### 8.1 端口被占用

```bash
# 检查端口占用（Mac/Linux）
lsof -i :3000
lsof -i :8080
lsof -i :8083

# 杀死占用进程
kill -9 <PID>

# Windows
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### 8.2 数据库连接失败

1. 检查MySQL服务是否启动
2. 检查数据库用户名密码是否正确
3. 检查防火墙设置
4. 检查数据库是否已创建

### 8.3 WebSocket连接失败

1. 检查WebSocket服务器是否启动
2. 检查端口8080是否被占用
3. 检查防火墙是否允许WebSocket连接
4. 检查代理配置是否正确

### 8.4 API请求失败

1. 检查后端服务是否启动
2. 检查代理配置是否正确
3. 检查CORS配置
4. 查看浏览器控制台和网络请求

## 9. 性能优化建议

### 9.1 前端优化

- 使用代码分割（Code Splitting）
- 启用Gzip压缩
- 使用CDN加速静态资源
- 启用浏览器缓存

### 9.2 后端优化

- 数据库连接池配置
- Redis缓存热点数据
- API响应压缩
- 数据库索引优化

### 9.3 数据库优化

- 合理设计索引
- 定期优化表结构
- 使用读写分离（如需要）
- 定期备份数据

## 10. 安全配置建议

### 10.1 生产环境安全

1. **HTTPS配置**: 使用SSL证书启用HTTPS
2. **CORS配置**: 限制允许的域名
3. **SQL注入防护**: 使用参数化查询
4. **XSS防护**: 对用户输入进行转义
5. **密码加密**: 使用BCrypt等加密算法
6. **Token过期**: 设置合理的JWT过期时间
7. **文件上传限制**: 限制文件类型和大小

### 10.2 环境变量安全

- 不要将 `.env` 文件提交到版本控制
- 生产环境使用环境变量或密钥管理服务
- 定期轮换密钥和密码

## 11. 监控与日志

### 11.1 日志配置

- 前端: 使用浏览器控制台和错误监控服务
- 后端: 使用Logback或Log4j2
- WebSocket: 记录连接和消息日志

### 11.2 监控指标

- 服务器CPU、内存使用率
- 数据库连接数和查询性能
- API响应时间
- WebSocket连接数
- 错误率和异常日志

## 12. 备份与恢复

### 12.1 数据库备份

```bash
# 备份数据库
mysqldump -u root -p exam_management > backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u root -p exam_management < backup_20240101.sql
```

### 12.2 文件备份

- 定期备份 `uploads/` 目录
- 备份监控数据（如需要）
- 使用自动化备份脚本

## 13. 总结

本系统采用前后端分离架构，包含前端React应用、后端Spring Boot服务、WebSocket实时通信服务和MySQL数据库。开发环境使用Mock服务器和WebSocket服务器进行本地开发，生产环境需要部署到服务器并配置相应的反向代理和负载均衡。

配置时需要注意：
1. 端口配置避免冲突
2. 环境变量正确设置
3. 数据库连接配置正确
4. 跨域和代理配置正确
5. 生产环境安全配置完善

