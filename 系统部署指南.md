# 考试管理系统部署指南

## 目录
1. [部署前准备](#部署前准备)
2. [本地开发环境部署](#本地开发环境部署)
3. [生产环境部署](#生产环境部署)
4. [使用Nginx部署](#使用nginx部署)
5. [使用PM2管理服务](#使用pm2管理服务)
6. [Docker容器化部署](#docker容器化部署)
7. [云服务器部署](#云服务器部署)
8. [部署验证](#部署验证)
9. [常见问题](#常见问题)

---

## 部署前准备

### 1. 系统要求

#### 服务器硬件要求
- **CPU**: 2核及以上
- **内存**: 4GB及以上（推荐8GB）
- **磁盘**: 20GB及以上可用空间
- **网络**: 稳定的网络连接

#### 软件环境要求
- **操作系统**: Linux (Ubuntu 20.04+ / CentOS 7+) / Windows Server / macOS
- **Node.js**: >= 14.0.0
- **Java**: >= 8 (JDK 8+)
- **MySQL**: >= 5.7 或 >= 8.0
- **Nginx**: >= 1.18 (可选，用于反向代理)

### 2. 安装必要软件

#### Linux (Ubuntu/Debian)
```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# 安装Java
sudo apt install openjdk-11-jdk -y

# 安装MySQL
sudo apt install mysql-server -y

# 安装Nginx
sudo apt install nginx -y

# 安装PM2
sudo npm install -g pm2
```

#### Linux (CentOS/RHEL)
```bash
# 更新系统
sudo yum update -y

# 安装Node.js
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 安装Java
sudo yum install java-11-openjdk java-11-openjdk-devel -y

# 安装MySQL
sudo yum install mysql-server -y
sudo systemctl start mysqld
sudo systemctl enable mysqld

# 安装Nginx
sudo yum install nginx -y

# 安装PM2
sudo npm install -g pm2
```

#### macOS
```bash
# 使用Homebrew安装
brew install node
brew install openjdk@11
brew install mysql
brew install nginx
npm install -g pm2
```

### 3. 准备项目文件

```bash
# 克隆或上传项目到服务器
# 如果使用Git
git clone <your-repo-url>
cd examination-management-system

# 或直接上传项目文件到服务器
```

---

## 本地开发环境部署

### 步骤1: 安装依赖

```bash
# 进入项目目录
cd /path/to/examination-management-system

# 安装前端依赖
npm install
```

### 步骤2: 配置环境变量

```bash
# 复制环境变量示例文件
cp env.example .env

# 编辑环境变量
nano .env
```

编辑 `.env` 文件：
```bash
# 后端API地址
BACKEND_API_URL=http://localhost:8080/api

# 数据库配置（如果需要）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=exam_management
DB_USER=root
DB_PASSWORD=your_password
```

### 步骤3: 配置数据库

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库
CREATE DATABASE exam_management 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_0900_ai_ci;

# 退出MySQL
exit;

# 导入表结构
mysql -u root -p exam_management < entry/sqlDDL/index.sql

# 导入初始数据（可选）
mysql -u root -p exam_management < entry/sqlDDL/insert_data.sql
```

### 步骤4: 启动服务

#### 方式一：一键启动（推荐）
```bash
# 同时启动前端、WebSocket和Mock API
npm run dev
```

#### 方式二：分别启动
```bash
# 终端1: 启动前端
npm start

# 终端2: 启动WebSocket服务器
npm run ws-server

# 终端3: 启动Mock API
npm run mock-api

# 终端4: 启动Spring Boot后端（如果有）
mvn spring-boot:run
```

### 步骤5: 访问应用

- **前端应用**: http://localhost:3000
- **后端API**: http://localhost:8083
- **WebSocket**: ws://localhost:8080/user-monitoring

---

## 生产环境部署

### 方案一：传统部署（推荐）

#### 1. 部署前端

```bash
# 1. 构建生产版本
npm run build

# 2. 构建产物在 build/ 目录
ls -la build/

# 3. 将build目录内容部署到Web服务器
```

#### 2. 部署后端

```bash
# 1. 进入后端项目目录（如果有单独的Spring Boot项目）
cd backend

# 2. 打包应用
mvn clean package

# 3. 生成的JAR文件在target目录
ls -la target/*.jar

# 4. 运行JAR文件
java -jar target/exam-management-system-1.0.0.jar
```

#### 3. 部署WebSocket服务器

```bash
# 1. 使用PM2启动WebSocket服务器
pm2 start websocket-server.js --name ws-server

# 2. 设置开机自启
pm2 startup
pm2 save

# 3. 查看状态
pm2 status
pm2 logs ws-server
```

---

## 使用Nginx部署

### 1. 配置Nginx

创建Nginx配置文件：

```bash
sudo nano /etc/nginx/sites-available/exam-system
```

配置文件内容：

```nginx
# 前端应用配置
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP
    
    # 前端静态文件目录
    root /var/www/exam-system/build;
    index index.html;
    
    # 前端路由支持（React Router）
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API代理到后端服务器
    location /api {
        proxy_pass http://localhost:8083;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时设置
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # WebSocket代理
    location /user-monitoring {
        proxy_pass http://localhost:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        
        # WebSocket超时设置
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
    
    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }
    
    # 上传文件访问
    location /uploads {
        alias /var/www/exam-system/uploads;
        expires 7d;
        add_header Cache-Control "public";
    }
    
    # Gzip压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript 
               application/x-javascript application/xml+rss 
               application/javascript application/json;
}
```

### 2. 启用配置

```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/exam-system /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重新加载Nginx
sudo systemctl reload nginx
```

### 3. 配置HTTPS（可选但推荐）

```bash
# 安装Certbot
sudo apt install certbot python3-certbot-nginx -y

# 获取SSL证书
sudo certbot --nginx -d your-domain.com

# 自动续期
sudo certbot renew --dry-run
```

---

## 使用PM2管理服务

### 1. 安装PM2

```bash
npm install -g pm2
```

### 2. 启动服务

```bash
# 启动WebSocket服务器
pm2 start websocket-server.js --name ws-server

# 启动后端API服务器（如果是Node.js）
pm2 start mock-server.js --name api-server

# 查看所有服务
pm2 list

# 查看日志
pm2 logs

# 查看特定服务日志
pm2 logs ws-server
```

### 3. PM2配置

创建 `ecosystem.config.js` 文件：

```javascript
module.exports = {
  apps: [
    {
      name: 'ws-server',
      script: './websocket-server.js',
      instances: 1,
      exec_mode: 'fork',
      env: {
        NODE_ENV: 'production',
        BACKEND_API_URL: 'http://localhost:8080/api'
      },
      error_file: './logs/ws-server-error.log',
      out_file: './logs/ws-server-out.log',
      log_date_format: 'YYYY-MM-DD HH:mm:ss Z',
      merge_logs: true,
      autorestart: true,
      max_memory_restart: '1G'
    },
    {
      name: 'api-server',
      script: './mock-server.js',
      instances: 1,
      exec_mode: 'fork',
      env: {
        NODE_ENV: 'production',
        PORT: 8083
      },
      error_file: './logs/api-server-error.log',
      out_file: './logs/api-server-out.log',
      autorestart: true
    }
  ]
};
```

使用配置文件启动：

```bash
pm2 start ecosystem.config.js
```

### 4. PM2常用命令

```bash
# 启动服务
pm2 start app.js --name my-app

# 停止服务
pm2 stop my-app

# 重启服务
pm2 restart my-app

# 删除服务
pm2 delete my-app

# 查看状态
pm2 status

# 查看详细信息
pm2 show my-app

# 监控
pm2 monit

# 保存当前进程列表
pm2 save

# 设置开机自启
pm2 startup
```

---

## Docker容器化部署

### 1. 创建Dockerfile

#### 前端Dockerfile

创建 `Dockerfile.frontend`:

```dockerfile
# 构建阶段
FROM node:18-alpine AS builder

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 构建应用
RUN npm run build

# 运行阶段
FROM nginx:alpine

# 复制构建产物
COPY --from=builder /app/build /usr/share/nginx/html

# 复制Nginx配置
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
```

#### WebSocket服务器Dockerfile

创建 `Dockerfile.websocket`:

```dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制package文件
COPY package*.json ./

# 安装依赖
RUN npm ci --only=production

# 复制源代码
COPY websocket-server.js ./

EXPOSE 8080

CMD ["node", "websocket-server.js"]
```

### 2. 创建docker-compose.yml

```yaml
version: '3.8'

services:
  # 前端服务
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "80:80"
    depends_on:
      - backend
      - websocket
    networks:
      - exam-network

  # 后端API服务
  backend:
    build:
      context: ./backend  # 如果有单独的Spring Boot项目
    ports:
      - "8083:8083"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/exam_management
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=rootpassword
    depends_on:
      - mysql
    networks:
      - exam-network

  # WebSocket服务
  websocket:
    build:
      context: .
      dockerfile: Dockerfile.websocket
    ports:
      - "8080:8080"
    environment:
      - BACKEND_API_URL=http://backend:8083/api
    networks:
      - exam-network

  # MySQL数据库
  mysql:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=rootpassword
      - MYSQL_DATABASE=exam_management
      - MYSQL_CHARACTER_SET_SERVER=utf8mb4
      - MYSQL_COLLATION_SERVER=utf8mb4_0900_ai_ci
    volumes:
      - mysql-data:/var/lib/mysql
      - ./entry/sqlDDL:/docker-entrypoint-initdb.d
    networks:
      - exam-network

volumes:
  mysql-data:

networks:
  exam-network:
    driver: bridge
```

### 3. 构建和运行

```bash
# 构建镜像
docker-compose build

# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 停止服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v
```

---

## 云服务器部署

### 1. 阿里云/腾讯云部署

#### 步骤1: 购买和配置服务器

1. 购买云服务器（推荐配置：2核4GB）
2. 配置安全组规则：
   - 开放端口：80, 443, 8080, 8083, 3306（仅内网）
   - 开放SSH端口：22

#### 步骤2: 连接服务器

```bash
# 使用SSH连接
ssh root@your-server-ip
```

#### 步骤3: 安装软件环境

按照"部署前准备"章节安装必要软件。

#### 步骤4: 上传项目文件

```bash
# 方式1: 使用SCP
scp -r /local/path/to/project root@your-server-ip:/opt/exam-system

# 方式2: 使用Git
git clone <your-repo-url> /opt/exam-system

# 方式3: 使用FTP工具（如FileZilla）
```

#### 步骤5: 配置和启动服务

按照"生产环境部署"章节配置和启动服务。

### 2. 域名配置

```bash
# 1. 购买域名
# 2. 配置DNS解析
#    - A记录: @ -> 服务器IP
#    - A记录: www -> 服务器IP

# 3. 在Nginx配置中使用域名
# 修改 server_name 为你的域名
```

---

## 部署验证

### 1. 检查服务状态

```bash
# 检查端口占用
netstat -tulpn | grep -E '3000|8080|8083|3306'

# 检查进程
ps aux | grep -E 'node|java|nginx|mysql'

# 检查PM2服务
pm2 status

# 检查systemd服务
sudo systemctl status nginx
sudo systemctl status mysql
```

### 2. 测试API接口

```bash
# 测试登录接口
curl -X POST http://localhost:8083/auth/login \
  -H "Content-Type: application/json" \
  -d '{"studentId":"test","password":"123"}'

# 测试WebSocket连接
# 使用浏览器控制台或WebSocket客户端工具
```

### 3. 测试前端访问

```bash
# 在浏览器访问
http://your-domain.com
# 或
http://your-server-ip
```

### 4. 检查日志

```bash
# Nginx日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# PM2日志
pm2 logs

# 应用日志
tail -f /opt/exam-system/logs/*.log
```

---

## 常见问题

### 问题1: 端口被占用

**解决方法**:
```bash
# 查找占用端口的进程
sudo lsof -i :8080
sudo netstat -tulpn | grep 8080

# 杀死进程
sudo kill -9 <PID>
```

### 问题2: 权限不足

**解决方法**:
```bash
# 修改文件权限
sudo chown -R www-data:www-data /var/www/exam-system
sudo chmod -R 755 /var/www/exam-system
```

### 问题3: 数据库连接失败

**检查清单**:
1. MySQL服务是否启动: `sudo systemctl status mysql`
2. 数据库是否创建
3. 用户名密码是否正确
4. 防火墙是否允许连接

**解决方法**:
```bash
# 启动MySQL
sudo systemctl start mysql

# 检查MySQL状态
sudo systemctl status mysql

# 测试连接
mysql -u root -p -h localhost
```

### 问题4: Nginx 502错误

**可能原因**:
- 后端服务未启动
- 代理配置错误
- 端口不匹配

**解决方法**:
```bash
# 检查后端服务
pm2 status
curl http://localhost:8083/health

# 检查Nginx配置
sudo nginx -t
sudo systemctl reload nginx
```

### 问题5: WebSocket连接失败

**检查清单**:
1. WebSocket服务器是否启动
2. Nginx WebSocket代理配置是否正确
3. 防火墙是否允许WebSocket连接

**解决方法**:
```bash
# 检查WebSocket服务
pm2 logs ws-server

# 测试WebSocket连接
# 在浏览器控制台运行
const ws = new WebSocket('ws://your-domain.com/user-monitoring');
```

### 问题6: 静态资源404

**解决方法**:
```bash
# 检查文件是否存在
ls -la /var/www/exam-system/build

# 检查Nginx配置
sudo nginx -t

# 检查文件权限
sudo chown -R www-data:www-data /var/www/exam-system
```

---

## 部署检查清单

### 部署前
- [ ] 服务器硬件满足要求
- [ ] 软件环境已安装
- [ ] 项目文件已上传
- [ ] 环境变量已配置
- [ ] 数据库已创建和初始化

### 部署中
- [ ] 前端已构建
- [ ] 后端已打包
- [ ] 服务已启动
- [ ] Nginx已配置
- [ ] 防火墙规则已配置

### 部署后
- [ ] 所有服务正常运行
- [ ] API接口可访问
- [ ] WebSocket连接正常
- [ ] 前端页面可访问
- [ ] 日志正常记录
- [ ] 监控已配置

---

## 性能优化建议

### 1. 前端优化
- 启用Gzip压缩
- 使用CDN加速静态资源
- 配置浏览器缓存
- 代码分割和懒加载

### 2. 后端优化
- 配置数据库连接池
- 使用Redis缓存
- API响应压缩
- 数据库索引优化

### 3. 服务器优化
- 配置合适的Nginx worker进程数
- 启用HTTP/2
- 配置SSL/TLS
- 定期清理日志

---

## 备份和恢复

### 数据库备份

```bash
# 备份数据库
mysqldump -u root -p exam_management > backup_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u root -p exam_management < backup_20240101.sql

# 自动备份脚本
#!/bin/bash
BACKUP_DIR="/backup/mysql"
DATE=$(date +%Y%m%d_%H%M%S)
mysqldump -u root -p$MYSQL_PASSWORD exam_management > $BACKUP_DIR/backup_$DATE.sql
# 保留最近7天的备份
find $BACKUP_DIR -name "backup_*.sql" -mtime +7 -delete
```

### 文件备份

```bash
# 备份上传文件
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz uploads/

# 备份监控数据
tar -czf monitoring_backup_$(date +%Y%m%d).tar.gz user-monitoring-data/
```

---

## 总结

本部署指南涵盖了从本地开发环境到生产环境的完整部署流程。根据实际需求选择合适的部署方案：

- **开发测试**: 使用本地开发环境部署
- **小规模部署**: 使用传统部署方案 + Nginx + PM2
- **大规模部署**: 使用Docker容器化部署
- **云服务部署**: 结合云服务器特性进行部署

部署完成后，定期检查服务状态、监控系统性能、备份重要数据，确保系统稳定运行。

